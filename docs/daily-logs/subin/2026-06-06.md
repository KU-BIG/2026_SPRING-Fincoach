# subin 일일 로그 - 2026-06-06

## 오늘 한 일

- 이슈 #23: `/api/chat/stream` SSE 스트리밍 엔드포인트 구현
  - `api/chat.py`: `_stream_llm()` 제너레이터 추가
    - `anthropic.messages.stream()` 으로 토큰 단위 SSE 스트리밍
    - API 키 없으면 데모 모드 메시지 fallback (503 대신 스트림 유지)
    - LLM 실패 시 1회 재시도 후 에러 이벤트 전송
    - 면책 문구는 스트림 마지막에 항상 보장
  - `web/src/lib/api.ts`: `chatStream()` 메서드 추가 (fetch + ReadableStream)
  - `web/src/pages/Chat.tsx`: 스트리밍 전환, 토큰 도착 시 실시간 버블 업데이트
  - `tests/test_api.py`: 스트리밍 테스트 2개 추가 (데모 모드, 토큰+면책)
  - ruff lint (SIM117, I001) 수정
- pytest 37/37 전체 통과
- PR #28 생성

## 다음 작업 (내일 할 것)

- 채팅 히스토리 세션 유지 (localStorage, 새로고침 후에도 유지)
- `ANTHROPIC_API_KEY` 실제 키 설정 후 end-to-end 스트리밍 동작 확인
- PR #28 머지 후 이슈 #23 close 확인

## 막힌 것

없음

## 브랜치

feature/subin-streaming

## 커밋

97870fd [chat] ruff lint 수정 (SIM117, I001)
f113693 [chat] 응답 스트리밍 (SSE) + 에러 처리 (#23)
