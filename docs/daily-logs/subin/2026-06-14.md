# subin 일일 로그 - 2026-06-14

## 오늘 한 일

- `/start-day` 컨텍스트 복구
  - main 71커밋 최신화 (Supabase Auth 기반 PR #122, market API PR #121 등 대규모 변경)
  - `feature/subin-site-api-integration` → main rebase (충돌 없음)
  - PR #116 CLOSED(미머지) 확인 — 커밋은 현 브랜치에 살아있음
- 이슈 #124 (P1) 작업: 유저별 채팅 기록 저장·복원
  - `web/src/pages/Chat.tsx`에 Supabase `chat_messages` 연동
    - `useAuth()` + `userRef` 패턴 (keydown 리스너 stale closure 방지)
    - 로그인 시 기록 복원 useEffect (`.eq("user_id", user.id)` + `.limit(100)`)
    - `sendMessage` 완료 후 user/assistant 2행 insert (acc 빈 문자열 skip)
    - `clearChat` 시 DB delete
    - 비로그인/데모 빌드는 기존 시드 쇼케이스 유지
  - `Chat.test.tsx`: AuthProvider 래핑 추가
  - `npm install` → `@supabase/supabase-js` 설치 (package.json에 있었으나 미설치)
  - `npm run build` TypeScript 빌드 통과
- Gemini 코드 리뷰 4개 수정 (별도 커밋)
  - SELECT `.eq("user_id", user.id)` 명시 필터 추가 (RLS 단독 의존 제거)
  - `acc=""` 빈 응답 DB insert 방지 (`&& acc` 조건 추가)
  - insert/delete `.then(({ error }) => console.error(...))` 에러 핸들러 추가
  - `chat.html` SSE 스트림 `[DONE]` 없이 종료 시 chatHistory 보정
- PR #126 생성 (`feature/subin-chat-history-db`)
  - closes #111, #112, #124

## 다음 작업 (내일 할 것)

- PR #126 리뷰 피드백 반영 (병승 리뷰 대기)
- 스모크 테스트: 백엔드 켠 상태에서 `python tests/smoke_test_llm.py` 실행 검증
- 이슈 #123 확인: 현태 포트폴리오 이슈와 연계 작업 필요 여부 파악

## 막힌 것

- `npm test` 전체 실행 불가: jsdom/`@exodus/bytes` ESM 충돌 — main 기존 이슈, 내 변경과 무관

## 브랜치

feature/subin-chat-history-db (PR #126)

## 커밋

51a6359 [web/chat] Gemini 리뷰 4개 수정
dc3b9ba [web] 유저별 채팅 기록 Supabase 저장·복원 (closes #124)
3772fcf [docs] subin daily log 2026-06-12
41ed77d [chat/web] /site/ 실 API 연결 + 루트 리다이렉트 + LLM 스모크 테스트 (closes #111, #112)
3c036b0 [docs] subin daily log 2026-06-14
