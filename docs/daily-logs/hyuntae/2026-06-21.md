# hyuntae 일일 로그 - 2026-06-21

## 오늘 한 일
- Issue #123: 유저별 포트폴리오 백엔드 + 프론트 전체 구현
- `portfolio_analyzer/analyzer.py`: holdings 파라미터 추가, ticker 기반 currency/market 자동 판별
- `api/portfolio.py`: POST /api/portfolio/summary, /analysis 엔드포인트 추가 (유저별 캐시)
- `web/src/lib/api.ts`: HoldingInput, postJSON, postPortfolioSummary, postPortfolioAnalysis
- `web/src/pages/Portfolio.tsx`: HoldingsForm(종목 입력 폼), UserSummaryCard(결과 테이블), Supabase holdings CRUD
- 캐시 키 버그 수정: ticker만 → ticker+shares+avg_price (수량/단가 변경 시 재분석)
- holdings 삭제 동기화: 폼에서 제거한 종목을 Supabase에서도 delete
- tests/test_api.py: POST 테스트 4개 추가 (총 15개 통과)
- Portfolio.test.tsx: AuthProvider 래퍼 추가 (12개 통과)
- 빌드 정상 확인
- PR #133 생성 → Gemini 리뷰 받음
- Gemini 리뷰 반영 (버그 수정 6건):
  - `if holdings:` → `if holdings is not None:` (빈 배열 구분)
  - 캐시 크기 제한 추가 (1000건 초과 시 clear, OOM 방지)
  - user 변경 시 상태 초기화 (holdingsLoaded/holdingRows/userSummary)
  - 신규 유저(빈 holdings)도 holdingsLoaded=true 설정
  - upsert 먼저 → delete 나중에 (실패 시 데이터 유실 방지)
  - 빈 포트폴리오 저장 가능하도록 버튼 disabled 조건 수정
  - eslint-disable 제거 (CI lint 에러 수정)

## 다음 작업 (내일 할 것)
- 차트 동적화 이슈 분리 (hero card, KPI, 도넛, 라인차트를 유저 데이터와 연동)
- 에러 피드백 UI 추가 (Supabase/API 실패 시 사용자 알림)

## 막힌 것
- npm 캐시 권한 문제 (sudo 불가) → HOME=/tmp로 우회
- hero card/KPI/도넛/라인차트가 하드코딩 데모 데이터라 유저 데이터 반영 불가 → 별도 이슈로 분리 예정

## 브랜치
feature/hyuntae-user-portfolio

## 커밋
8a24751 [portfolio] 유저별 포트폴리오 입력/저장/계산/분석 (#123)
(pending) [portfolio] Gemini 리뷰 반영: 버그 수정 6건
